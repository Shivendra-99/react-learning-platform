// Placeholder — swap for the real domain once this is deployed, and
// regenerate public/sitemap.xml (npm run generate:sitemap).
export const SITE_URL = "https://react-learn.example.com"
export const SITE_NAME = "React Learn"
export const DEFAULT_DESCRIPTION =
  "Learn React for free with short, visual lessons and a live code editor in every one — components, hooks, state management, routing, and more."
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png`
